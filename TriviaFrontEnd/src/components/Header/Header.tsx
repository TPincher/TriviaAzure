import styles from "./Header.module.scss";

const Header = () => {
  return (
    <div className={styles.container}>
      <p className={styles.header}>- - - - TRIVIA - - - -</p>
    </div>
  );
};

export default Header;
